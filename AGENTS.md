# Atlas ELN 开发与发布规则

本文件适用于整个仓库。所有自动化代理和开发协作者都必须遵守以下规则。

## 任务路由与上下文控制

开始工作前先按任务类型读取最近的子目录规则，不要默认扫描整个仓库：

- 页面、交互、客户端领域逻辑或应用服务：读取 `app/AGENTS.md`；
- API 接口：读取 `app/AGENTS.md` 和 `app/api/AGENTS.md`；
- 数据表、数据库访问或初始化数据：读取 `db/AGENTS.md`；
- 数据库结构变更：同时读取 `db/AGENTS.md` 和 `drizzle/AGENTS.md`；
- 新增或修改测试：读取 `tests/AGENTS.md` 和被测功能的局部规则；
- Docker、Nginx、部署、备份或回滚：读取 `deploy/AGENTS.md`；
- 测试任务先读取被测功能的相邻实现和对应测试，不要为了单个测试浏览所有业务目录。

默认从任务直接涉及的目录、入口文件和测试开始搜索。只有发现明确的跨模块调用、共享类型、数据库影响或安全边界时，才扩大到相邻模块。子目录 `AGENTS.md` 只补充局部规则；本文件中的安全、Git、测试和发布要求始终有效。

## 基本原则

- 功能开发、问题修复、数据库结构调整和依赖升级必须先在本地完成。
- 不得直接在生产服务器上编写、修改或试验代码。
- 不得把生产服务器作为开发环境或首次测试环境。
- 未经用户明确要求，不得主动连接、更新或重启生产服务器。
- `.env`、密码、令牌、数据库备份、附件和真实用户数据不得提交到 Git。

## 新功能开发前检查

开始实现前先形成简短的影响清单，并据此限定首轮阅读范围：

- 明确验收标准、目标用户、允许执行操作的角色和失败时的用户反馈；
- 判断数据属于浏览器本地偏好、团队服务器数据、附件实体还是审计记录；
- 确认是否影响 API 契约、数据库 Schema、迁移、备份格式、导入导出或部署配置；
- 确认是否涉及登录、权限、密码、Cookie、并发版本、审计或不可信文件；
- 列出直接入口、共享模型、持久化层和对应测试，只有存在证据时才扩大范围；
- 确定需要新增的单元、接口、数据库或浏览器验证，不能只依赖生产构建通过；
- 若改变公开行为、数据边界、部署步骤或安全假设，同步更新相应文档。

新增生产依赖前，应说明现有依赖无法满足的原因、运行时影响和维护风险，并取得用户同意。

## 本地开发流程

1. 开始开发前确认当前分支和工作区状态，保留用户已有的未提交修改。
2. 从最新的 `main` 创建独立功能分支；分支名建议使用 `feature/`、`fix/` 或 `chore/` 前缀。
3. 在本地完成实现，不修改已经发布的历史数据库迁移文件。
4. 数据库结构变化必须生成新的 `drizzle/*.sql` 迁移，并检查其向前兼容性和回滚影响。
5. 完成后至少运行：

   ```bash
   npm test
   npm run lint
   ```

6. `npm test` 必须通过。Lint 不得出现新增错误；新增警告也应处理或在交付说明中明确解释。
7. 涉及 Docker 或部署配置时，还要验证 Compose 配置，并在可行时进行本地容器启动与健康检查。
8. 测试失败时继续在本地修复，不得把未通过验证的版本部署到服务器。

## 按变更类型验证

- 纯领域逻辑：增加或更新对应单元测试，再运行完整 `npm test`；
- UI 与交互：验证加载、空状态、失败、冲突、键盘操作和常用窗口尺寸；有浏览器自动化时运行对应用例，否则记录人工冒烟步骤和结果；
- API：覆盖未登录、无权限、非法输入、正常读写、冲突和错误响应；不能只依赖页面间接验证；
- 数据库：验证空数据库执行全部迁移，以及从上一生产基线带数据升级；检查约束、幂等和团队隔离；
- 登录、账户、附件和权限：增加边界与滥用场景验证，确认审计、Cookie、大小限制和路径安全；
- Docker 或部署：验证 Compose 解析、容器健康检查和运行文档与配置一致性。

如果仓库暂时缺少某一层自动化测试基础设施，不得把“没有现成测试”当作跳过验证的理由。优先补最小可执行回归测试；无法自动化时，必须在交付说明中记录可复现的人工验证步骤、结果和剩余风险。

## Git 与代码同步

- 只提交本次任务相关文件，不覆盖或清理用户的无关改动。
- 提交前检查暂存文件，确认没有环境配置、密钥、数据库文件、附件或生成目录。
- 功能分支测试通过并完成必要审查后，才能合并到 `main`。
- 生产服务器只同步已经推送到远程仓库的正式提交，不通过压缩包或临时文件覆盖源码。
- 服务器使用 `git pull --ff-only`，禁止强制拉取、强制覆盖或在生产目录解决开发冲突。

## 服务器发布门槛

只有同时满足以下条件，才可以建议或执行服务器更新：

- 本地测试和生产构建通过；
- 正式提交已经推送到远程仓库；
- 用户明确要求部署或同步服务器；
- 已确认目标服务器、部署目录、分支和待发布提交号；
- 发布前已备份 PostgreSQL 数据库和附件；
- 已准备上一版本提交号或镜像以及可执行的回滚方案。

标准发布顺序为：

1. 检查服务器当前状态和磁盘空间；
2. 备份数据库与附件；
3. 执行 `git pull --ff-only`；
4. 重新构建并启动 Web 服务；
5. 检查容器状态、日志和 `/api/health`；
6. 验证登录、数据读取、保存和附件等核心功能；
7. 发现异常时停止继续发布，并按预案回滚。

## 生产数据安全

- 禁止执行 `docker compose down -v`。
- 禁止删除 PostgreSQL 或附件数据卷来解决启动问题。
- 禁止使用开发机 `.env` 覆盖服务器 `.env`。
- 禁止修改或覆盖已经执行过的迁移文件；必须新增迁移。
- 涉及不兼容数据库迁移时，代码回滚必须与数据库恢复配套进行。

## 交付说明

每次功能开发完成后，应向用户明确报告：

- 修改了哪些功能和文件；
- 本地运行了哪些测试及结果；
- 是否包含数据库迁移或配置变化；
- 是否已经提交、推送或部署；
- 若尚未部署，应明确说明服务器仍保持原版本。
